import * as TerraconnectUI from 'terraconnect-ui';
import Card from 'Components/UI/Cards/Card';
import CardGrid from 'Components/UI/Cards/CardGrid';

export default (
  <>
    <CardGrid gap={10} margin={10}>
      <Card width={1} height={1}>Test</Card>
    </CardGrid>
  </>
);